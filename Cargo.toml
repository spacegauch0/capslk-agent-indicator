[package]
name = "capslock-indicator"
version = "0.1.0"
edition = "2021"
description = "Claude Code agent status indicator using the Caps Lock LED"
license = "MIT"

[dependencies]
serde_json = "1"

[profile.release]
strip = true
lto = true
opt-level = "z"
